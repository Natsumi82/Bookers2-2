<h1>New Book</h1>
<%= form_with model: @book do |f| %>
 <h4>Title</h4>
 <%= f.text_field :title %>

 <h4>Opnion</h4>
 <%= f.text_area :body %>

<h4>Image</h4>
 <%= f.file_field :image, accept: "image/*" %>

 <%= link_to 'Create book', new_book_path, class: "btn btn-secondary" %>

<% end %>



