<% if user.profile_image.attached? %>
    <%= image_tag user.profile_image, class:"border border-secondary rounded-circle", size:"100x100" %>
<% else %>
    <%= image_tag 'no_image', size: "100x100" %>
<% end %>